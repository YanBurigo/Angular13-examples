import { TaskDoneDirective } from './task-done.directive';

describe('TaskDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDoneDirective(null);
    expect(directive).toBeTruthy();
  });
});
